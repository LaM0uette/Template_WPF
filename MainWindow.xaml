<mah:MetroWindow x:Class="Template_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="Template_WPF"
        Width="{Binding Source={StaticResource AppWidth}}" 
        MinWidth="{Binding Source={StaticResource AppWidth}}"
        Height="{Binding Source={StaticResource AppHeight}}" 
        MinHeight="{Binding Source={StaticResource AppHeight}}"
        ShowTitleBar="True"
        ResizeMode="CanResizeWithGrip"
        WindowTransitionsEnabled="False"
        WindowStartupLocation="CenterScreen"
        TitleAlignment="Center"
        Icon="{StaticResource ImgMain}"
        GlowBrush="{StaticResource RgbM1}"
        WindowTitleBrush="{StaticResource RgbM1}"
        >
    
    <mah:MetroWindow.Resources>
        <Style x:Key="CustomLightMaxButtonStyle"
               BasedOn="{StaticResource MahApps.Styles.Button.MetroWindow.Light}"
               TargetType="{x:Type Button}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="DarkGoldenrod" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="Yellow" />
                    <Setter Property="Foreground" Value="Black" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CustomLightCloseButtonStyle"
               BasedOn="{StaticResource MahApps.Styles.Button.MetroWindow.Close.Light.Win10}"
               TargetType="{x:Type Button}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="DarkRed" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="Red" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </mah:MetroWindow.Resources>

    <mah:MetroWindow.WindowButtonCommands>
        <mah:WindowButtonCommands 
            Minimize="Réduire"
            Maximize="Agrandir"
            Restore="Rétrécir"
            Close="Quitter"
            LightMinButtonStyle="{StaticResource MinButtonStyle}"
            LightMaxButtonStyle="{StaticResource CustomLightMaxButtonStyle}"
            LightCloseButtonStyle="{StaticResource CustomLightCloseButtonStyle}" 
            DarkMinButtonStyle="{StaticResource MinButtonStyle}"
            DarkMaxButtonStyle="{StaticResource CustomLightMaxButtonStyle}"
            DarkCloseButtonStyle="{StaticResource CustomLightCloseButtonStyle}" 
            />
    </mah:MetroWindow.WindowButtonCommands>
    
    <Grid>

    </Grid>
    
</mah:MetroWindow>
